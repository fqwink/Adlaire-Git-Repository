# Auris / Adlaire Git Repository - 最上位ルールブック

## 0. 絶対原則

`AGENTS.md` 自体が、本リポジトリにおける絶対原則である。

このリポジトリで作業するすべてのエージェントは、すべての作業において、必ず最初に本ファイル `AGENTS.md` を読むこと。これは本リポジトリの最優先絶対原則であり、調査、設計、ドキュメント改訂、実装、検証、Git 操作、Pull Request 作成、レビュー対応、リリース判断を含む全作業に適用する。

`AGENTS.md` を読まずに作業へ着手してはならない。作業途中で新しい指示、追加作業、方針変更、修正依頼が発生した場合も、必要に応じて `AGENTS.md` を再確認し、本ファイルのルールを最優先して適用する。

`AGENTS.md` を読んだだけで、作業判断に必要な確認を完了したと扱ってはならない。

すべての作業では、`AGENTS.md` を最初に読んだ後、2類ポリシーで定義される読了対象と読了条件に従うこと。読了義務を満たすまで、作業判断、変更、実装、検証、Git 操作、Pull Request 作成へ進んではならない。

`AGENTS.md` を読んだ後に参照すべきドキュメント一覧は、`docs/DOCUMENT_INDEX.md` を索引として確認する。`docs/DOCUMENT_INDEX.md` は参照索引であり、`AGENTS.md`、2類ポリシー、3類マスター仕様書の代替ではない。

ドキュメント整合性は、1類ルールブック、2類ポリシー、3類マスター仕様書、マスター開発計画、マスター実装機能候補リスト、README、実装、テスト、検証導線、バージョン表記、Pull Request 説明を含む全体へ適用する。いずれか一部に限定して扱ってはならない。

新しい作業指示を受けた場合は、最上位ドキュメント読了工程と同時に、直前作業の Pull Request merge 状態、作業ブランチのクローズ状態、ローカルとリモートの整合性を確認する。

`docs/tpl-governance/` は、AdlaireGroup 関連プロジェクト、プロダクトへ展開する共通ガバナンス雛形の正本である。

本リポジトリの変更が AdlaireGroup 共通方針に該当する場合は、`docs/tpl-governance/` の更新要否を必ず判定し、該当する場合は同一変更範囲で雛形も整合させる。

本ファイル `AGENTS.md` は、本リポジトリにおける **最上位ルールブック** である。

本プロジェクトは **Adlaire Group 内部向け Git ホスティング基盤** を構築するためのリポジトリである。実装・設計・修正・検証・リリースに関する判断は、個別ファイルの都合よりも最上位ドキュメント体系を優先する。

---

## 0.1 ルールブックとポリシーの役割

`AGENTS.md` は、作業者が必ず従う絶対原則、禁止事項、優先順位、承認条件、作業手順の上位ルールを定義するルールブックである。

`AGENTS.md` は、2類ポリシーおよび3類マスター仕様書の上位に位置し、ポリシーや仕様書の内容を統制する。ただし、2類ポリシーの責務範囲に属する詳細条件、判定基準、運用方針を重複して詳細定義する文書ではない。

2類ポリシーは、`AGENTS.md` に従う下位文書として、ドキュメント体系、開発プロセス、技術要件、バージョン、リリース、テスト、ライセンス等の責務別方針、判定条件、運用手順を定義する。

2類ポリシーは、`AGENTS.md` の絶対原則に違反してはならない。`AGENTS.md` と2類ポリシーが矛盾する場合は、`AGENTS.md` を正とし、2類ポリシーを修正して整合させる。

`AGENTS.md` に置くべき内容は、全作業に適用される絶対原則、禁止事項、優先順位、承認義務、上位参照順序に限る。責務別の詳細な条件、判定基準、手順、例外管理は、該当する2類ポリシーを正本として定義する。

### 0.1.1 役割混同の再発防止

`AGENTS.md` と2類ポリシーの役割を混同してはならない。

過去に、2類ポリシーで定義すべき詳細条件、読了対象、判定基準、運用手順を `AGENTS.md` に重複して詳細記載した誤りがあった。この誤りを繰り返してはならない。

`AGENTS.md` を改訂する場合は、追加内容が絶対原則、禁止事項、優先順位、承認義務、上位参照順序に該当するかを必ず確認する。該当しない詳細条件、判定基準、運用手順、例外管理は `AGENTS.md` に追加せず、該当する2類ポリシーへ記載する。

2類ポリシーを改訂する場合は、`AGENTS.md` の絶対原則に従いつつ、責務別の詳細を自分の責務範囲で定義する。2類ポリシーの詳細を `AGENTS.md` に戻す、または `AGENTS.md` と2類ポリシーへ同じ詳細を二重管理することを禁止する。

役割混同を発見した場合は、上位下位の関係だけで判断せず、まず内容の責務を判定する。全作業に適用される絶対原則は `AGENTS.md` へ、責務別の詳細条件と運用手順は該当する2類ポリシーへ移し、両者の整合を取る。

### 0.1.2 承認工程省略の再発防止

変更を加える作業では、承認工程を省略してはならない。

エージェントは、変更前に必ず変更対象、変更理由、変更案、影響範囲、検証方法を提示し、ユーザー承認を得なければならない。提示または提案を行わずに、ユーザー発言を承認済み変更範囲として扱ってはならない。

承認後に限り、承認済み範囲の編集、検証、承認済み範囲だけの stage、commit、push、Pull Request 作成または更新を自動実行してよい。

承認済み範囲を超える変更、方針の意味変更、ソースコード実装、依存関係・技術採用・バージョン固定、Pull Request merge、`main` 反映、作業ブランチ削除、破壊的 Git 操作は、必ず別途承認を得る。

承認工程の詳細条件、自動化してよい範囲、別承認が必要な範囲は、2類ポリシーに詳細記載する。まず `AGENTS.md` に絶対原則を記載し、その後、該当する2類ポリシーに詳細を記載して整合させる。

## 1. 最上位原則

### 1.1 最上位ドキュメント体系

本プロジェクトの最上位ドキュメントは、**1類**、**2類**、**3類** に分類する。

**1類は最上位中の最上位であり、2類および3類より常に優先する。**

| 類 | 位置づけ | 役割 |
|---:|---|---|
| 1類 | ルールブックのルールブック群 | 開発・運用・判断・禁止事項・手順・優先順位を定義する |
| 2類 | ドキュメント憲章・ポリシー群 | ドキュメント体系、開発プロセス、バージョニング、リリース判定、依存関係、セキュリティ等の方針を定義する |
| 3類 | マスター仕様書のマスター仕様書群 | プロダクト仕様、システム仕様、機能仕様、非機能仕様を定義する |

参照対象の一覧、読了対象、各ドキュメントの役割は `docs/DOCUMENT_INDEX.md` を索引として確認する。ただし、同索引は最上位ドキュメント体系の類に属する正本ではなく、参照導線を整理する補助文書である。

### 1.2 1類: ルールブックのルールブック群

1類ドキュメントは、すべての作業判断において最優先される。

| 優先順位 | ドキュメント | 役割 |
|---:|---|---|
| 1 | `AGENTS.md` | 本リポジトリの最上位ルールブック。全ルールブック、全ポリシー、全仕様書の上位に位置する |

1類ドキュメント間に矛盾がある場合は、優先順位の高いドキュメントを正とする。

### 1.3 2類: ドキュメント憲章・ポリシー群

2類ドキュメントは、ドキュメント体系を定義するドキュメント憲章と、開発・運用・品質・リリース・セキュリティ等の方針を定義するポリシー群である。

| 優先順位 | ドキュメント | 役割 |
|---:|---|---|
| 1 | `docs/policies/DEVELOPMENT_POLICY_RULEBOOK.md` | 2類ドキュメント群の総則。ドキュメント憲章と責務別ポリシーの参照順序、共通原則、変更手順を定義する |
| 2 | `docs/policies/DOCUMENT_CHARTER.md` | ドキュメント憲章。ドキュメント体系、責務境界、開発計画、フェーズ単位の改訂方針 |
| 3 | `docs/policies/TECHNICAL_REQUIREMENTS_POLICY.md` | 採用技術、ランタイム、依存関係、固定採用バージョン、技術承認の方針 |
| 4 | `docs/policies/VERSION_POLICY.md` | バージョン表記、累積方式、Major / Minor 更新条件の方針 |
| 5 | `docs/policies/RELEASE_POLICY.md` | 安定版リリース、リリース対象、リリース禁止条件の方針 |
| 6 | `docs/policies/DEPLOYMENT_POLICY.md` | デプロイ、運用基盤、本番サーバ反映、バックアップ、検証、ロールバックの方針 |
| 7 | `docs/policies/TEST_POLICY.md` | 意味のあるテスト、テストピラミッド、テスト追加・削除判断の方針 |
| 8 | `docs/policies/LICENSE_POLICY.md` | クローズドライセンス、権利帰属、外部公開、ライセンス変更の方針 |

2類ドキュメントは、ドキュメント憲章と責務ベースで独立したポリシーとして管理する。ドキュメント憲章は文書体系と責務境界を定義し、各ポリシーは自分の責務範囲を正本として扱い、他責務の詳細を重複定義してはならない。

2類ドキュメント間に矛盾がある場合は、優先順位の高いポリシーを正とする。ただし、責務別ポリシーの責務範囲に属する詳細については、その責務別ポリシーを優先して整合させる。

### 1.3.1 マスター開発計画

開発計画は、以下のマスター開発計画で管理する。

| ドキュメント | 役割 |
|---|---|
| `docs/plans/DEVELOPMENT_PLAN.md` | マスター開発計画。フェーズ単位の開発計画、フェーズ別バージョン、実装対象、対象外、検証範囲、完了条件を定義する |
| `docs/plans/MASTER_IMPLEMENTATION_FEATURE_CANDIDATES.md` | マスター実装機能候補リスト。優先実装候補、保留候補、候補選定基準を定義する |

`docs/plans/DEVELOPMENT_PLAN.md` は、本プロジェクトの **マスター開発計画** であり、1類ルールブック、2類ドキュメント群、3類マスター仕様書に従う。

`docs/plans/MASTER_IMPLEMENTATION_FEATURE_CANDIDATES.md` は、本プロジェクトの **マスター実装機能候補リスト** であり、今後の実装候補を整理するための正本である。ただし、同ファイルは実装承認ではない。候補を実装対象へ確定する場合は、3類マスター仕様書とマスター開発計画へ反映し、ユーザー承認を得る。

マスター実装機能候補リストは、機能候補、優先候補、保留候補、候補選定理由を管理する。正式仕様、実装対象、対象外、Phase 割り当て、完了条件、検証範囲は、3類マスター仕様書とマスター開発計画で管理する。

マスター開発計画は、3類マスター仕様書に基づいて策定する。フェーズ構成、実装対象、対象外、検証範囲、完了条件、リリース判定条件は、マスター仕様書に定義された仕様を実装順序へ落とし込むために定義する。

計画内容または候補リストが上位ドキュメントと矛盾する場合は、上位ドキュメントを正とし、マスター開発計画またはマスター実装機能候補リストを修正する。

### 1.3.2 AdlaireGroup 共通雛形の正本

`docs/tpl-governance/` は、AdlaireGroup 関連プロジェクト、プロダクトへ展開する共通ガバナンス雛形の正本である。

`docs/tpl-governance/` 配下は、本プロジェクト固有の正本ドキュメントではなく、AdlaireGroup 共通方針を他プロジェクトへ展開するための正本雛形として管理する。

採用技術、固定バージョン、ライセンス、デプロイ方式、データ保存、依存関係、セキュリティ要件は、AdlaireGroup 共通方針として扱う。プロジェクトごとに必要な固有値、配置先、ドメイン、サーバ名、仕様書名、Phase 計画、実装対象機能は、共通方針を破らない範囲で具体化する。

本プロジェクトの `AGENTS.md`、2類ポリシー、3類マスター仕様書、マスター開発計画を変更する場合は、その変更が AdlaireGroup 共通方針に該当するかを必ず判定する。該当する場合は `docs/tpl-governance/` も同一変更範囲で更新し、該当しない場合は雛形へ反映しない理由を Pull Request 説明に記載する。

### 1.4 3類: マスター仕様書のマスター仕様書群

3類ドキュメントは、仕様に関する最上位ドキュメント群である。

3類マスター仕様書は、本プロジェクトにおける仕様の **正本** である。機能、構造、非機能要件、セキュリティ要件、実装範囲は、必ずマスター仕様書に基づいて判断する。

| 優先順位 | ドキュメント | 役割 |
|---:|---|---|
| 1 | `docs/specs/Auris_System_Design.md` | システム全体のマスター仕様書。機能範囲、アーキテクチャ、非機能要件、セキュリティ方針の基準 |
| 2 | `docs/specs/Adlaire_Git_Repository_Specification.md` | Adlaire Git Repository 本体のマスター仕様書 |
| 3 | `docs/specs/Adlaire_Deploy_Specification.md` | Adlaire Git Repository 公式付随システムである Adlaire Deploy のマスター仕様書 |
| 4 | `README.md` | 利用者向け手順、開発・デプロイ補足 |

3類ドキュメント間に矛盾がある場合は、優先順位の高いマスター仕様書を正とする。

### 1.5 1類・2類・3類の関係

- 1類は、2類と3類を管理・制御する上位ルールである。
- 2類は、1類に違反してはならない。
- 3類は、1類および2類に違反してはならない。
- 仕様判断は3類を基準にする。
- 3類マスター仕様書は仕様の正本であり、開発はマスター仕様書に基づいて行う。
- 開発手順、禁止事項、リリース判定、バージョン規約、優先順位判断は1類を基準にする。
- ドキュメント体系、開発プロセス、依存関係、品質、リリース、セキュリティの方針判断は2類を基準にする。
- 1類、2類、3類が矛盾する場合は、より上位の類を正とし、下位の類を修正して整合させる。

### 1.6 実装着手条件

`AGENTS.md`、2類ドキュメント群、`docs/specs/Auris_System_Design.md`、`docs/plans/DEVELOPMENT_PLAN.md` が整備されていない場合、エージェントは実装作業に着手してはならない。

この場合に許可される作業は以下に限る。

- 1類ルールブックの作成・整理
- 2類ポリシーの作成・整理
- 3類マスター仕様書の作成・整理
- マスター開発計画の作成・整理
- マスター実装機能候補リストの作成・整理
- 既存仕様書の `docs/` 配下への移動・統合
- ドキュメント間の矛盾整理
- リポジトリ構成の初期整備
- ユーザーから明示された調査・レビュー

### 1.7 ソースコード実装の承認原則

ソースコード実装作業は、必ずユーザーの承認を得てから着手することを絶対原則とする。

開発言語、ランタイム、データベース、Git、外部コマンド、外部ライブラリ、フレームワーク、採用バージョン、固定バージョン、更新方針、リリース方針など、開発・実装・運用に影響する技術判断は、すべてユーザーが決定する。

エージェントは候補提示、比較、調査、リスク整理、推奨案の提示を行ってよい。ただし、ユーザーの明示承認なしに採用決定、バージョン固定、方針確定、実装反映を行ってはならない。

全ての作業において、承認を得る義務は他の都合より優先される。効率、一般的な慣習、既存実装との整合、検証済みであることを理由に、承認を省略してはならない。

ここでいうソースコード実装作業には、以下を含む。

- アプリケーションコードの作成・変更・削除
- テストコードの作成・変更・削除
- ビルド設定、実行設定、依存関係設定の作成・変更・削除
- データベース schema、migration、seed の作成・変更・削除
- Web UI、CSS、JavaScript、TypeScript、Deno 実行コードの作成・変更・削除

承認前に許可される作業は、調査、レビュー、設計整理、ルールブックの整備、ポリシーの整備、マスター仕様書の整備、マスター開発計画の整備、マスター実装機能候補リストの整備、実装計画の提示に限る。

ユーザーがソースコード実装を承認した場合、エージェントは3類マスター仕様書および `docs/plans/DEVELOPMENT_PLAN.md` の対象フェーズに基づいて実装しなければならない。承認後に、マスター仕様書と承認済みフェーズ計画で定義された実装範囲を理由なく放置してはならない。

---

## 2. 開発対象と技術方針

### 2.1 プロジェクト概要

本プロジェクトは、以下を提供するセルフホスト型 Git ホスティング基盤である。

基本的な機能互換は GitHub 互換基準とする。GitHub 互換とは、Repository、Issue、Pull Request、Code Review、Wiki、Release、Webhook、REST API、Personal Access Token、Organization / Team、Projects、Discussions 等の機能体系、用語、主要ワークフローについて、GitHub 利用経験を前提に理解・移行しやすい振る舞いを目指すことを指す。

ただし、UI、画面デザイン、画面レイアウト、視覚表現は GitHub 互換の対象外とする。UI は本プロジェクト独自の設計とし、GitHub の画面、デザイン、ブランド表現、商標表現を模倣しない。

GitHub 互換はマスター仕様書に定義された範囲に限る。GitHub の全機能、外部サービス依存、商標・ブランド表現、GitHub 固有サービスへの直接依存を無条件に採用してはならない。現時点で不要な機能は、GitHub に存在する機能であっても実装対象から除外する。

オープンソースの Git プロバイダーやセルフホスト型 Git ホスティング製品は、サブの機能互換インスパイア対象として扱ってよい。ただし、これらは主たる互換基準ではない。Gitea、Forgejo、GitLab、GitPrep 等の機能、画面、API、運用モデルをそのまま互換対象として扱ってはならず、採用する場合は本プロジェクトのマスター仕様書に再定義し、必要性を確認し、ユーザー承認を得る。

- Git clone / push / pull / fetch
- Branch / Tag / Release 管理
- ユーザー登録、ログイン、認証、権限管理
- SSH key / HTTP Basic Auth / API token
- Repository CRUD、Visibility 制御
- Web UI によるリポジトリ閲覧・管理
- 操作ログ、Webhook、README 表示

### 2.2 基本技術スタック

3類マスター仕様書で変更されない限り、以下を標準技術スタックとする。

| 領域 | 技術 |
|---|---|
| Runtime | Deno |
| Language | TypeScript |
| HTTP | `Deno.serve` |
| Database | libSQL |
| Git 操作 | `Deno.Command` |
| Frontend | HTML / CSS / Vanilla JavaScript |
| Packaging | Deno single binary |

採用バージョンは、原則として各技術の **最新の安定版** を採用方針とする。

Deno、SQLite、libSQL、Git、Deno 標準ライブラリ、Deno で利用する外部コマンド、例外採用する外部ライブラリ、その他ユーザー承認を得て採用する技術は、採用または更新の時点で公式情報を確認し、最新の安定版を採用候補とする。

TypeScript は **6系の最新安定版** を採用方針とする。Deno に同梱される TypeScript を利用する場合も、TypeScript 6系の最新安定版であることを採用条件とする。

承認済み固定採用バージョンは以下とする。

| 技術 | 固定採用バージョン | 扱い |
|---|---:|---|
| Deno | `v2.9.5` | 標準ランタイム |
| TypeScript | `v6.0.3` | 6系の最新安定版として採用 |
| SQLite | `v3.53.4` | 互換・移行元・検証用データベース |
| libSQL | `libsql-server v0.24.32` | 標準データベース |
| Git | `v2.55.0` 系 | Git 操作用外部コマンド |
上記にない技術、Deno 標準ライブラリの個別モジュール、Deno で利用する外部コマンド、例外採用する外部ライブラリの固定バージョンは、個別にユーザー承認を得るまで未確定とする。

固定採用バージョンを更新する場合は、採用または更新の時点で公式情報を確認し、ユーザー承認を得てからマスター開発計画、3類マスター仕様書、実装設定へ反映する。

Node.js ランタイムは、セキュリティ上のリスクによるプロジェクト方針として採用を禁止する。

本番サーバ運用、デプロイ、運用基盤では、Deno single binary 形式と Docker 形式の双方を標準化対象とする。

Deno single binary を正本成果物とする。Docker は正本成果物ではなく、Deno single binary を Docker image に同梱して実行する運用選択肢の一つである。

Docker を使用する場合も、Docker を使用せず Deno single binary を host OS 上で直接実行する場合も、同じ system / data 分離構成にする。Deno single binary、Docker image、container、起動管理定義は差し替え可能な system 側として扱う。libSQL / SQLite database、Git bare repositories、config、secrets、logs、backups、manifests は保護対象 data 側として扱い、host filesystem を正本として system 側から分離する。

Docker を利用する場合も、Node.js runtime、npm ecosystem、`npm:` specifier、`package.json`、`node_modules` を導入してはならない。

本番サーバ環境へのデプロイは、承認工程を省かず、バックアップ、検証、ロールバック前提を含めて自動化を標準とする。詳細は `docs/policies/DEPLOYMENT_POLICY.md` を正本とする。

### 2.2.1 標準運用基盤

Adlaire Git Repository 本体の標準運用基盤は、self-host、VPS、専用サーバーを前提とする。

最小本番構成は、1 VPS 上に差し替え可能な system 側と host filesystem による data 側を同居させる構成とする。Docker を使用する場合も、Docker を使用しない binary 直実行の場合も、この構成を変えてはならない。Git ホスティング本体は、Git bare repository の永続保存、`git` コマンド実行、ファイルシステム、容量管理、バックアップ、復旧、権限管理を中核とする。そのため、data 側は container lifecycle に依存させず、host filesystem 上で直接保全できる構成を基準にする。

Deno Deploy、Turso Cloud、その他 libSQL 系クラウドDBサービスは、標準採用ではなく将来候補として保留する。採用を検討する場合は、3類マスター仕様書とマスター開発計画へ採用理由、対象範囲、対象外、リスク、検証範囲を反映し、ユーザー承認を得る。

Deno Deploy を採用候補にする場合も、Node.js runtime、npm ecosystem、`npm:` specifier、`package.json`、`node_modules` を前提にしてはならない。

Turso Cloud 等のクラウドDBサービスを採用候補にする場合も、Database Gateway と内製 `libsql` driver の接続先差し替えとして扱い、アプリケーション上位層へサービス固有APIやサービス名を露出してはならない。

### 2.3 依存関係ポリシー

原則として、外部依存は必要最小限に抑える。

- フレームワークは、内製したもの以外の採用を禁止する。
- 内製フレームワークを採用する場合も、1類ルールブックおよび2類ポリシーに違反せず、3類マスター仕様書に採用理由・責務範囲・保守方針を記載する。
- Deno 標準ライブラリを優先する。
- Deno 標準ライブラリの個別モジュールを採用する場合も、必要性、対象モジュール、固定バージョン、検証方法を提示し、ユーザー承認を得る。
- JSR レジストリの公開ライブラリは採用可能とする。ただし、ユーザー承認を得るまで採用してはならない。JSR は Deno / TypeScript / ESM と相性がよいが、承認なしの外部依存導入経路として扱ってはならない。
- JSR レジストリの公開ライブラリであっても、npm 互換、`npm:` specifier、`package.json`、`node_modules`、Node.js runtime、npm ecosystem への依存を前提とするものは採用禁止とする。
- JSR へ公開する package は、公開可能なオープンソースコードであることを前提とする。クローズドライセンス、内部専用、非公開資産は JSR へ公開してはならない。
- クローズドな Adlaire 内製 Deno package の配布は、短期的には Private Git + Deno import、Deno workspace、vendor 管理を候補とし、中長期計画として Adlaire 内製 Deno Module Registry を Adlaire Git Repository 本体へ早期実装する方針とする。
- npm registry 互換レジストリは、Node.js / npm ecosystem リスクと衝突するため標準採用しない。
- 外部ライブラリを採用する場合は、必要最小限であることを前提とし、必ず **例外採用** として指定したうえでユーザー承認を得る。
- 例外採用した外部ライブラリは、アプリケーション全体へ直接露出させず、内製ラッパー、内製driver、または内製Gatewayの内部に閉じ込める。外部ライブラリを使う場合も、設計上は内製化された境界を通して利用する。
- Node.js ランタイムの採用は禁止する。
- npm パッケージ、Node.js 前提ツール、webpack、jest 等の導入は禁止する。ただし、実行時ランタイムに Node.js を含まず、Deno 方針を破らない開発補助用途に限り、外部ツールの例外採用として1類ルールブックおよび2類ポリシーで許可し、3類マスター仕様書に承認理由を記載し、ユーザー承認を得てから行う。
- セキュリティ・暗号・認証に関わる処理を独自実装する場合は、3類マスター仕様書にリスクと検証方針を記載する。

---

## 3. ドキュメント運用ルール

### 3.1 仕様変更の順序

機能追加、設計変更、非互換変更、技術スタック変更を行う場合は、必ず以下の順序で進める。

1. 1類ルールブックに違反しないことを確認する
2. 2類ポリシーで定義された読了義務を満たす
3. `docs/specs/Auris_System_Design.md` を更新する
4. 必要に応じて3類マスター仕様書を更新する
5. `docs/plans/DEVELOPMENT_PLAN.md` の対象フェーズ、対象バージョン、実装対象、対象外、検証範囲を確認または更新する
6. 2類ドキュメント群のうち、ドキュメント憲章と作業責務に対応する責務別ポリシーに照らして作業種別を判定する
7. ソースコード実装作業についてユーザー承認を得る
8. 3類マスター仕様書と承認済みフェーズ計画に基づいて実装する
9. テスト・検証結果を記録する

3類マスター仕様書にない機能を独断で実装してはならない。

### 3.1.1 フェーズ完了時のリポジトリ整合性義務

各フェーズは、実装またはドキュメント更新が完了しただけでは完了扱いにしてはならない。

フェーズ単位で、必ずドキュメント等の整合性向上を行う。これは任意の品質改善ではなく、本リポジトリのルールであり、ポリシーである。

フェーズ完了前に、リポジトリ全体の整合性確認と整合性向上を必ず行う。1類ルールブック、2類ポリシー、3類マスター仕様書、マスター開発計画、マスター実装機能候補リスト、実装、テスト、検証導線、バージョン表記、README、Pull Request 説明の間に矛盾がある場合は、矛盾を補正してからでなければフェーズ完了として扱ってはならない。

リポジトリ整合性確認は、各フェーズの完了条件に必ず含める。

リポジトリ整合性が取れていない状態で、次のフェーズに進んではならない。次フェーズへ移行する前に、前フェーズのドキュメント、仕様、計画、ポリシー、実装、テスト、検証導線、バージョン表記、PR説明の整合性向上を完了していなければならない。

### 3.2 既存仕様書の扱い

ルート直下の仕様書は、正式運用では3類マスター仕様書として `docs/specs/` 配下へ統合する。

移行対象の例:

- `Adlaire-Git-Repository.md` -> `docs/specs/Adlaire_Git_Repository_Specification.md`

移行後は、ルート直下に重複する仕様書を残さない。必要であれば README から正式ドキュメントへリンクする。

### 3.3 矛盾発見時の対応

ドキュメント間に矛盾がある場合は、類と優先順位の高いドキュメントを正とする。

ただし、以下の矛盾を見つけた場合は実装前に整理する。

- Deno only 方針と Node.js ランタイムまたは Node/npm 前提ツールの衝突
- 外部依存必要最小限方針と外部ライブラリ導入の衝突
- バージョン表記の不一致
- Phase 1 / Phase 2 / Phase 3 の機能範囲の不一致
- セキュリティ要件の不足または曖昧さ

---

## 4. バージョン・リリース規約

### 4.1 バージョン表記

バージョン表記は `docs/policies/VERSION_POLICY.md` に定義する `v.{Major}.{Minor}` 形式に従う。

フェーズ番号は累積方式とし、安定版リリース後もリセットしてはならない。

初回安定版リリース前の正式バージョンは `v.0.{Minor}` とする。安定版未リリース中は、Phase が進んでも `Major` を `0` のまま維持する。

`Major` は安定版リリース系列を表す。`Major` が `1` 以上のバージョンは、安定版リリース系列として扱う。

`Minor` はプロジェクト全体の累積変更番号とし、`Major` 更新時またはフェーズ変更時もリセットしてはならない。

未安定版の例:

```text
v.0.1
v.0.12
```

安定版リリース系列の例:

```text
v.1.13
v.2.23
```

`package.json`、`deno.json`、ドキュメント本文、リリースノートで別形式を使う場合は、正式バージョンとの対応関係を明記する。

### 4.2 リリース禁止条件

安定版リリースフェーズは、累積フェーズ番号における 7系フェーズをデフォルト方針とする。

7系フェーズとは、Phase 7、Phase 17、Phase 27 のように、累積フェーズ番号の下一桁が 7 となるフェーズを指す。

Phase 5、Phase 15、Phase 25 のような 5系フェーズは、補助的なリリース判定、設計・デザイン改良、仕様整理のためのフェーズとして扱い、安定版リリースフェーズとして扱ってはならない。

Phase 6、Phase 16、Phase 26 のような 6系フェーズは、大規模なバグ修正とドキュメント整合性向上をデフォルト方針とする安定版リリース準備フェーズとして扱う。必要に応じて移行準備と検証強化も行う。6系フェーズ自体は安定版リリースフェーズではない。

Phase 9、Phase 19、Phase 29 のような 9系フェーズは、補助的なリリース判定フェーズとして扱う。9系フェーズはケースバイケースで安定版リリースフェーズになる場合と、ならない場合がある。

以下に該当する場合はリリースしてはならない。

- 7系フェーズではなく、かつ9系フェーズとして安定版リリース対象に明示承認されていない
- 既知のバグが残っている
- セキュリティ要件の検証が未完了
- Git 操作、認証、権限管理、データ永続化の主要テストが未完了
- 主要仕様に対する意味のあるテストまたは代替検証による確信を説明できない
- マイグレーション手順またはロールバック手順が未定義
- 1類ルールブック、2類ポリシー、または3類マスター仕様書と実装が矛盾している

---

## 5. 実装ルール

### 5.1 実装前承認

ソースコード実装作業は、ユーザーの明示的な承認なしに開始してはならない。

実装承認を求める前に、最上位ドキュメント全体を読了していなければならない。未読または一部未読の状態で、実装対象、仕様根拠、変更予定ファイル、検証予定を提示してはならない。

承認を得る際は、最低限以下を提示する。

- 実装対象
- 根拠となる3類マスター仕様書
- 変更予定ファイルまたは作成予定ファイル
- 実施予定の検証

承認後は、3類マスター仕様書を正本として実装する。

### 5.2 作業順序

作業は原則として以下の優先順で行う。

1. バグ修正
2. セキュリティ修正
3. 機能改良
4. 追加機能
5. リファクタリング
6. 将来構想

ただし、ユーザーが明示した場合、または正式リリース計画で順序が定義されている場合はそちらを優先する。

### 5.3 実装方針

- 既存構成・命名・責務分離に合わせる。
- 変更範囲を必要最小限に保つ。
- 3類マスター仕様書を正本として、定義済み仕様に基づいて実装する。
- 認証、権限、Git 操作、DB 更新は特に慎重に扱う。
- ユーザー入力は必ず検証・エスケープする。
- Web UI は外部フレームワークを使わず、HTML / CSS / Vanilla JavaScript を基本とする。
- Web UI は GitHub 互換の対象外とし、本プロジェクト独自のUIとして設計する。
- フレームワークが必要な場合は内製のみとし、外部ライブラリが必要な場合は例外採用としてユーザー承認を得る。
- Git リポジトリの実データと libSQL / SQLite メタデータの整合性を壊す変更を行わない。

### 5.4 禁止事項

- ユーザー承認なしのソースコード実装作業
- 3類マスター仕様書にない機能の独断実装
- 1類ルールブック、2類ポリシー、または3類マスター仕様書を無視した設計変更
- 承認済みフェーズ計画を無視した実装
- 内製したもの以外のフレームワーク採用
- 例外採用として指定せず、ユーザー承認を得ていない外部ライブラリ採用
- ユーザーの未確認変更の巻き戻し
- 認証・認可を迂回するショートカット実装
- 本番向けコードへのデバッグ用資格情報の埋め込み
- 秘密鍵、API token、パスワード、個人情報のコミット
- `main` への直接 push
- 破壊的 Git 操作の無断実行

---

## 6. テスト・検証ルール

### 6.1 テストの基本原則

テストは手段であり、目的は仕様が壊れていないという確信を得ることである。

意味のないテストを作成してはならない。意味のあるテストのみを作成・維持する。

意味のあるテストとは、最低限以下を満たすテストである。

- 失敗した時に、何が壊れたのかを教える。
- テストが落ちた瞬間に、どの仕様または振る舞いが壊れたのかを特定できる。
- 実装を変えても、外から見た仕様が同じなら生き残る。
- 内部の作りには触れず、外から見た振る舞いだけを検証する。
- 3類マスター仕様書またはマスター開発計画のどの要求を守るためのテストか説明できる。

確信にも説明にも寄与しない冗長なテストは削除する。

テスト構成は、土台に大量の高速なユニットテスト、中間に統合テスト、頂点に少数のエンドツーエンドテストを置くピラミッド構成を原則とする。

詳細なテストポリシーは `docs/policies/TEST_POLICY.md` に従う。

### 6.2 必須検証領域

以下の領域に変更を加えた場合は、該当テストを必ず追加または更新する。

- Git Smart HTTP
- SSH key 認証
- HTTP Basic Auth
- API token
- Repository CRUD
- Visibility / permission
- libSQL / SQLite schema / migration
- Webhook
- Audit log
- XSS 対策

### 6.3 検証コマンド

正式実装後は、原則として以下の導線を維持する。

```bash
deno task fmt
deno task lint
deno task test
deno task compile
```

コマンドが存在しない初期段階では、作業内容に応じて代替検証を行い、最終報告で未実施理由を明記する。

---

## 7. セキュリティ原則

### 7.1 認証・認可

- 認証と認可を明確に分離する。
- private repository へのアクセスは、すべての入口で権限確認する。
- API token は平文保存しない。
- Password / token / secret はログに出力しない。
- SSH key は形式・長さ・重複を検証する。

### 7.2 Git 操作

- `Deno.Command` に渡す引数は配列で扱い、shell 展開に依存しない。
- リポジトリ名、ブランチ名、タグ名、パスは検証する。
- パストラバーサルを禁止する。
- bare repository と作業ディレクトリの境界を明確にする。

### 7.3 Web UI

- HTML 出力は必ずエスケープまたはサニタイズする。
- 画像、リンク、埋め込み URL は許可スキームを制限する。
- Markdown / README 表示時の XSS を防止する。

---

## 8. Git 運用

### 8.1 main 保護

`main` への直接 push は全面禁止とし、例外を認めない。

すべての変更は、作業ブランチから Pull Request を作成し、PR 経由で `main` に取り込むことを必須事項とする。

リモート、ローカルの状態に関係なく、変更を `main` に取り込む正式経路は Pull Request のみとする。

運用方針は以下の一点に集約する。

- `main` へ直接 push せず、必ず PR 経由にする。

この運用において、`main` への反映は GitHub 上の Pull Request を作成し、その Pull Request をマージして、リモートリポジトリ GitHub の `main` に取り込むことだけを正式経路とする。

ローカル `main` への反映は、GitHub の `main` に取り込まれた後、その結果を取得して同期する作業に限る。

### 8.2 ブランチ

エージェントがブランチを作成する場合は、原則として `codex/` prefix を使用する。

作業ブランチを継続している場合、エージェントは必ず `AGENTS.md` を読んでから作業を行うこと。

作業ブランチで変更を行った場合は、承認済みの変更範囲だけをコミットし、作業ブランチをリモートへ push し、必ず Pull Request を作成すること。

作業ブランチを継続している限り、ローカルコミットのみで作業を止めてはならない。`main` へ直接 push せず、作業ブランチの push と PR 作成をもって `main` への取り込み経路とする。

作業ブランチは、Pull Request がマージされたら自動的に閉じる方針とする。

Pull Request マージ後は、リモート作業ブランチを削除し、ローカル `main` を GitHub の `main` と同期したうえで、ローカル作業ブランチも削除する。

Pull Request の確認とマージ作業はユーザーが行う。エージェントは、Pull Request 作成後、ユーザーからマージ完了通知がない場合でも、後続作業の最上位ドキュメント読了工程と同時に Pull Request のマージ完了を確認し、マージ済みであれば作業ブランチのクローズとローカル・リモート整合性確認を行う。

例:

```text
codex/init-design-documents
codex/phase1-repo-crud
codex/fix-auth-permission-check
```

### 8.3 コミット

コミットはユーザーから明示された場合のみ行う。

以後このリポジトリで変更した場合は、承認済みの変更範囲だけを stage してコミットまで行う。

コミット前には以下を確認する。

- 作業ツリーにユーザー由来の無関係変更が混在していないか
- stage 対象がユーザーに承認された変更範囲だけであるか
- 仕様ドキュメントと実装が矛盾していないか
- 検証コマンドを実行したか
- 未実施テストがある場合、その理由を説明できるか

---

## 9. 初期整備タスク

このリポジトリが理想構成に到達するまで、以下を優先する。

1. `docs/` ディレクトリを作成する
2. `docs/policies/DEVELOPMENT_POLICY_RULEBOOK.md` と責務別ポリシー群を2類ポリシーとして作成する
3. `docs/specs/Auris_System_Design.md` を3類マスター仕様書として作成する
4. `docs/plans/DEVELOPMENT_PLAN.md` をマスター開発計画として作成する
5. 既存仕様書を3類マスター仕様書として `docs/specs/` 配下へ移行・統合する
6. Deno only 方針と Node.js ランタイム禁止方針を2類ポリシーおよび3類マスター仕様書へ反映する
7. Phase 1 の実装範囲と対応バージョンを確定する
8. `deno.json`、`src/`、`tests/`、`public/` の最小構成を作成する

---

## 10. エージェント応答ルール

エージェントは作業完了時に、以下を簡潔に報告する。

- 変更したファイル
- 実施した検証
- 未実施の検証と理由
- 次に必要な作業

設計上の不明点や矛盾を見つけた場合は、推測で実装せず、ドキュメント修正またはユーザー確認を優先する。
